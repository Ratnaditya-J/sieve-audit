# Methodology

The audit protocol (probe decodability -> surface baselines -> efficacy gate ->
matched-control steering -> two-judge behavioral scoring -> verdict) is specified
in [`../DESIGN.md`](../DESIGN.md), sections 3-5. This document will expand each
step into a runnable reference once the engine lands.

Origin: the controlled causal-sufficiency protocol from *"Probing Is Not Enough"*
(companion paper).
