# Reports

Generated audit cards land here. The first will be the Qwen3-32B L55 demo
(`qwen3_l55_demo_report.md`) - the canonical worked example referenced in the
README and DESIGN.

Audit cards contain aggregate results only (no prompt text), consistent with the
data policy in the root `.gitignore`.
