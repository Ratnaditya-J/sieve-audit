"""Adapters: turn external artifacts (published probe releases, benchmark
dumps, model-runner outputs) into SIEVE evidence bundles.

Adapters never ship third-party data; they consume locally downloaded
artifacts and emit a bundle JSON (see the repository data policy).
"""
